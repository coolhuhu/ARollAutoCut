# AGENTS.md

本文件定义在此仓库中工作的开发约定。所有自动化代理和贡献者都应遵守以下规则。

## 需求确认

- 修改任何文件前，必须先阅读相关代码、配置和文档。
- 必须与用户确认目标行为、交互流程、输入输出、边界条件、错误处理、兼容性和测试范围。
- 需求有歧义或实现方案存在明显取舍时，必须先提问，不得自行开始修改。
- 需求确认后，先提交可执行的实现和测试计划，等待用户明确同意后再编码。
- 需求确认阶段只允许进行只读检查。
- 实现中发现需求需要调整时，应暂停相关修改并重新获得确认。

## 技术栈与依赖

- 桌面应用使用 Tauri 2、React、TypeScript、Vite 和 Rust。
- JavaScript 依赖统一使用 npm 管理，提交 `package-lock.json`。
- Rust 依赖统一使用 Cargo 管理，提交 `src-tauri/Cargo.lock`。
- 不得混用 pnpm、Yarn 或其他包管理器。
- SenseVoice 模型不得提交到 Git。
- `src-tauri/resources/silero_vad.onnx` 是 App 内置资源，可以提交。
- FFmpeg 和 FFprobe sidecar 必须使用固定版本，并保留对应许可证信息。

## 编码要求

- 修改应聚焦已确认的需求，不得夹带无关重构。
- 遵循现有模块边界、命名、类型和错误处理方式。
- React 组件不直接承载复杂业务规则；可测试的规则应放在独立 TypeScript 模块或 Rust domain 模块中。
- Tauri command 只负责参数转换和调用 service，不应包含复杂业务逻辑。
- 长任务必须支持进度、取消和资源清理。
- 用户可见错误使用简体中文，并提供可操作的信息。

## 测试要求

- 所有代码修改都必须添加或更新相应的单元测试。
- 修复缺陷时必须补充能够覆盖该缺陷的回归测试。
- 前端使用 Vitest 和 React Testing Library。
- Rust 使用内置测试框架；外部模型或媒体测试标记为 ignored 集成测试。
- 单元测试不得依赖网络、大型模型或外部服务。
- 不得通过删除测试、降低断言或隐藏失败来获得通过结果。

提交前至少运行：

```bash
npm test
npm run build
cargo test --manifest-path src-tauri/Cargo.toml
```

涉及真实模型或媒体链路时，还应运行对应的 ignored 集成测试。

## 完成标准

只有同时满足以下条件，任务才算完成：

1. 实现与最终确认的需求一致。
2. 新增或修改行为有适当的单元测试。
3. 前端和 Rust 相关测试全部通过。
4. 构建、资源、依赖和锁文件保持一致。
5. 文档已同步更新。
6. 最终回复说明修改内容、测试命令和测试结果。
