# AGENTS.md

本文件定义在此仓库中工作的开发约定。所有自动化代理和贡献者都应遵守以下规则。

## 需求确认

- 在修改任何文件之前，必须先理解现有代码、项目结构和相关文档。
- 必须与用户充分确认需求，包括目标行为、输入输出、边界条件、错误处理、兼容性要求和测试范围。
- 如果需求存在歧义、缺少关键信息，或不同实现方案会产生明显取舍，必须先提出问题，不得自行开始修改。
- 需求明确后，应先给出简洁、可执行的实现与测试计划，并等待用户明确同意后再编码。
- 在需求确认阶段，只允许进行只读检查；不得创建、编辑或删除项目文件。
- 如果实现过程中发现原需求需要调整，应暂停相关修改，说明原因并重新获得用户确认。

## Python 与依赖管理

- 本项目使用 `uv` 管理 Python、虚拟环境和依赖。
- 不得直接使用 `pip install`、`python -m venv`、Poetry、Pipenv 或 Conda 修改项目环境。
- 添加或删除依赖时，应更新 `pyproject.toml`，并通过 `uv sync` 更新环境和 `uv.lock`。
- 运行 Python 命令、CLI、脚本和测试时，应优先使用 `uv run`，例如：

```bash
uv sync
uv run aroll-autocut --help
uv run pytest
```

- `pyproject.toml` 和 `uv.lock` 必须保持一致。除非用户明确要求，否则不得手工编辑 `uv.lock`。

## 编码要求

- 修改应聚焦已确认的需求，不得夹带无关重构或格式化。
- 遵循现有项目结构、命名方式、类型标注和错误处理风格。
- 新增行为必须处理已确认的边界条件，并提供清晰、可操作的错误信息。
- 不得提交模型文件、生成文件、缓存、虚拟环境或其他大体积运行产物。

## 测试要求

- 所有代码修改都必须添加或更新相应的单元测试。
- 修复缺陷时，必须先补充能够覆盖该缺陷的回归测试。
- 测试应覆盖正常流程、关键边界条件和预期错误路径。
- 单元测试不得无必要地依赖大型模型、网络或外部服务；此类验证应标记为集成测试。
- 涉及 VAD、ASR 或完整转写流程时，应在本地模型可用的情况下运行相关集成测试。
- 至少应运行与修改直接相关的测试；提交前应尽可能运行完整测试套件：

```bash
uv run pytest -m "not integration"
uv run pytest
```

- 测试失败时不得将任务标记为完成。必须修复代码或测试，并重新运行直至通过。
- 不得通过删除测试、降低断言强度、跳过有效测试或隐藏失败来获得通过结果。

## 完成标准

只有同时满足以下条件，任务才算完成：

1. 实现与用户最终确认的需求一致。
2. 所有代码修改均有适当的单元测试覆盖。
3. 相关测试和完整测试套件均已通过，或已明确说明无法运行的客观原因并获得用户接受。
4. 文档、依赖声明和锁文件已根据修改同步更新。
5. 最终回复应简要说明修改内容、执行过的测试及其结果。
